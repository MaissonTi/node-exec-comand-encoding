# node-exec-comand-encoding

File Encoding Type Verification Example.

# Pre-requisites
- Configure file index.js in line 23 variable PROJETO _FILES

# Usage
- To begin with, the following steps should be followed:
- Clone repository
- Run `npm install`
- Run `node index.js`
